// Web entry point. The Flutter web compiler reports this path (org-dartlang-app:/web_entrypoint.dart).
// This file exists so the IDE can open it when debugging or when the compiler references it.
// The actual app entry is lib/main.dart.

import 'package:dwce_time_tracker/main.dart' as app;

void main() => app.main();
