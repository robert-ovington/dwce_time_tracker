// Stub so the IDE/debugger can resolve org-dartlang-app:/web_entrypoint.dart
// (Flutter web DDC reports this path). The build uses lib/main.dart; this file
// delegates to it so "Open file" and breakpoints resolve correctly.

import 'package:dwce_time_tracker/main.dart' as app;

void main() => app.main();
